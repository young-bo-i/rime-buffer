import Foundation

/// User-facing brand identity. Compatibility identities deliberately remain
/// `RimeBuffer`/`ETInput` in bundle IDs, data paths, and update payload paths.
enum ProductIdentity {
    static let displayName = "RIMES"
}
